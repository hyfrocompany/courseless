node_modules/
out/
dist/
.vite/
*.log

# secrets — Supabase/Stripe keys for local development
.env.local
.env*.local
.env

# AI-harness tooling installed locally by `npx impeccable skills install` — not part of the app
.claude/
.agents/
.cursor/
.codex/

# scratch working directory used by the seed-generation harness
.codex-seed-workdir/

# generated by scripts/provision-stripe.sh — contains Stripe ids and the webhook signing secret
.stripe-provision.*.env
release/

# TypeScript incremental build cache
*.tsbuildinfo
